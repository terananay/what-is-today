<div class="uk-container uk-flex uk-flex-center uk-margin">
  <%= form_with url: login_path, data: { turbo_method: :post }, class: "uk-card uk-card-default uk-card-body" do |f| %>
    <div>
      <%= f.label :email %>
      <%= f.email_field :email, class: "uk-input" %>
    </div>
    <div class="field uk-margin">
      <%= f.label :password %>
      <%= f.password_field :password, class: "uk-input" %>
    </div>
    <div class="uk-flex uk-flex-center">
      <%= f.submit class: "uk-button uk-button-primary" %>
    </div>
  <% end %>
</div>
